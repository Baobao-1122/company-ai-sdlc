# AI / 研发协作入口

本仓库为 **{{PROJECT_NAME}}**。大模型切换协作时，**先读本文件与 `docs/`**，再改代码。

## 必读文档（按顺序）

| 文档 | 用途 |
|------|------|
| [docs/prd.md](docs/prd.md) | 产品目标、功能范围、验收标准 |
| [docs/dev-standard.md](docs/dev-standard.md) | 目录、代码规范（待建） |
| [docs/testing.md](docs/testing.md) | 测试与 Harness 门禁（待建） |
| [docs/deploy.md](docs/deploy.md) | 部署说明（待建） |

## 项目边界（硬性）

{{PROJECT_BOUNDARY}}

## 关键代码位置

{{KEY_PATHS}}

## 本地启动

{{LOCAL_START}}

## Agent 执行规程（按改动类型）

| 触发条件 | 必跑命令 |
|----------|----------|
| 修改数据库 schema（无数据库时删除本行或标 N/A） | migration-prepare: `{{DATABASE_MIGRATION_PREPARE_COMMAND}}`; migration-apply: `{{DATABASE_MIGRATION_APPLY_COMMAND}}`; structure-check: `{{DATABASE_STRUCTURE_CHECK_COMMAND}}`; api-smoke: `{{DATABASE_API_SMOKE_COMMAND}}` |
| 命中业务数据影响面（查询语义/归属/聚合/金额口径等；可选） | 只读对账：`{{DATA_RECONCILE_READONLY_COMMAND}}`（无脚本时可手工 SQL/导出对比；**禁止**写库；产出结论与问题清单） |

> 数据库项目必须填写真实可执行的 migration 生成/应用、结构检查与针对性 API 冒烟命令；缺失时不得通过 CP-02/CP-05。  
> 业务数据对账命令为可选登记：有则填写只读命令；无则允许手工核对，但校验过程仍禁止改删业务数据。

## AI-SDLC 协作（AI 引导，你在 CP 介入）

Agent 在 `⏸ CP-xx` 处输出 **决策包**：①目的 ②判断要点 ③自检 ④推荐 ⑤编号选项。

| 文档 | 用途 |
|------|------|
| [docs/qa/ai-sdlc-agent-active-guidance.md](docs/qa/ai-sdlc-agent-active-guidance.md) | 决策包模板 |
| [docs/qa/ai-sdlc-human-checkpoints.md](docs/qa/ai-sdlc-human-checkpoints.md) | CP 何时停 |
| [docs/qa/ai-sdlc-decision-rubrics.md](docs/qa/ai-sdlc-decision-rubrics.md) | 通用 Allow/Stop 判据 |
| [docs/qa/ai-sdlc-code-review.md](docs/qa/ai-sdlc-code-review.md) | 每次 commit 前 Review |
| [docs/qa/ai-sdlc-task-state.md](docs/qa/ai-sdlc-task-state.md) | 任务状态（换 Chat 可续） |

流程：**需求 → 设计 → 实现 → 验证 → 提交**；harness 后（若触发）**P1 Reflection**；提交前必跑 `test:harness:ci` + `sdlc-review`（**CP-06 Reflection**）。

**Task State：** 当前 feat 进度写入 `.sdlc/task-state.yaml`（Agent 自动维护）；新 Chat 说「续任务」或 Agent 自动 Read 恢复。

### Reflection 速查（v1.5+）

| 你应看到 | 含义 |
|----------|------|
| `🔄 Reflection · P1 …` | harness 后早期互查，无需回复 |
| `ℹ️ Reflection · P1 未触发` | 跳过 P1，正常进 CP-05 |
| `🔄 Reflection · CP-06 …` | commit 前最后一轮互查，无需回复 |

详见 [docs/qa/ai-sdlc-code-review.md](docs/qa/ai-sdlc-code-review.md) §11。

## CP 判断扩展

> 复制自 [docs/qa/ai-sdlc-cp-extension.template.md](docs/qa/ai-sdlc-cp-extension.template.md)，按项目填写。

| CP | 项目追加要点 | Allow | Stop |
|----|--------------|-------|------|
| CP-01 | {{CP01_EXTENSION}} | | |
| CP-02 | {{CP02_EXTENSION}} | | |
| CP-06 | {{CP06_EXTENSION}} | | |
| CP-10 | {{CP10_EXTENSION}} | | |

## Git 提交（硬性）

- **一 commit 一功能**
- 提交前必跑 `test:harness:ci` + `sdlc-review`（CP-06 Code Review）
- 用户未要求时不 `git push`

## 文档维护

需求或部署约定变更时，**同步更新对应 `docs/*.md`**，并在文档顶部「变更记录」追加一行。
