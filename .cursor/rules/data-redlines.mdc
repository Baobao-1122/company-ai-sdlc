---
description: 数据红线：生产库禁止擅自写操作；对账只读（始终生效）
alwaysApply: true
---

# 数据红线

AI 与研发在任务中**必须**遵守；违反视为 **CP-10 级**，默认 **Stop**。

## 生产环境数据库（硬性）

- **禁止**在没有 **CP-10 负责人明确授权** 的情况下，对**生产环境**数据库执行任何写操作：`INSERT` / `UPDATE` / `DELETE` / `DDL` / 在生产执行 migration / 清库 / 批修复 / 「顺手改一条」
- **禁止**将连接串、DMS、脚本或 Agent 命令默认指向生产并执行写操作
- **禁止**用户口头「帮我在生产改一下」即执行 — 须 **CP-10** 授权并留痕（任务/工单/对话确认）

**允许（非生产）：** 本地库与 CP-02 声明的**验收/隔离集成库**上，按 migration 链与测试需要读写；仍须遵守「对账只读」。

## 校验、对账、reconcile

- 抽样对账与数据校验 **只读**；产出 **结论与问题清单**
- **禁止**以校验、对账、reconcile、冒烟名义对业务库 **INSERT/UPDATE/DELETE**
- 发现数据错误：报告问题；修数 **另开任务**；写 **生产** 仍 **CP-10**

## 配套

- 暂停点：**CP-10**（见 `docs/human-checkpoints.md`）
- 判据：`docs/decision-rubrics.md` § CP-10、业务数据影响相关行
