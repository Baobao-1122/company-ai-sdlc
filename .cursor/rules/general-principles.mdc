---
description: 通用开发原则，适用于公司所有 Web 项目，始终生效
alwaysApply: true
---

# 通用开发原则

AI 在编写或修改代码时必须严格遵守。

## 圈复杂度与函数规模

单个函数/方法的圈复杂度不得超过 **10**。超过时必须拆分为多个具名函数。

**不以「单函数 50 行」作为公司硬指标**（商业化平台常有线性较长的 handler）；可选在项目 eslint 中设置 `max-lines-per-function`（建议 80～100）配合本条的复杂度约束。

## TypeScript 类型

- 禁止 `any`；确需逃逸时使用 `unknown` 并做类型收窄，须在 CP-06 说明理由
- 须通过项目 **L4 lint**（如 `@typescript-eslint/no-explicit-any`: error）

## 异常处理

- 禁止空 `catch`（如 `catch {}`）；须用项目 logger 记录或 rethrow，禁止静默吞错
- 须通过 lint（如 `no-empty` / 相关 TS 规则）

## 副作用与可变入参

- 默认不 mutate 函数入参；必须修改时须在命名或职责上明确（如 `applyPatch`）
- 纯计算/校验函数应避免修改全局状态

## 外部 npm 依赖

- 禁止为小事引入大体积或低维护库；优先现有技术栈
- **禁止**在未过 **CP-02 已列依赖且用户「开始实现」** 或 **CP-03「确认安装」** 前执行 `pnpm add` / `npm install <pkg>` / 修改 `package.json` 增加生产依赖

## Early Return 原则

- 禁止超过 **3 层**的 if 嵌套
- 所有前置条件校验必须使用 Early Return 提前退出
- 复杂条件判断提取为具名布尔变量

## 禁止硬编码（配置与字面量）

业务逻辑中禁止写死以下内容，须用 **环境变量 / 配置文件 / 具名常量 / 错误码字典**（项目约定路径，见 CP-02 设计）引用：

- **环境与密钥**：API Key、连接串、账号密码等（禁止字面量进仓库；用 `process.env` 等，并同步 `.env*.example` 与 `docs/deploy.md`）
- **业务常量**：阈值、枚举、状态码、超时、分页大小等（禁止魔法数字/字符串散落逻辑）
- **错误与提示文案**：对用户或 API 的错误说明须进集中定义（如 `lib/errors`、错误码表）；**不强制 i18n**，多语言要求写在项目 `AGENTS.md` §CP 判断扩展

仍遵守 **Early Return** 与 **具名常量**；与 `docs-sync` Rule 联动：新增/变更 env 须更新部署文档与 example。

## 禁止废弃代码

- 禁止提交注释掉的代码块，直接删除
- 禁止保留未使用的变量、import、函数
- `console.log` 禁止出现在提交的代码中

## 单文件行数限制

- 单个文件不超过 **300 行**（新代码）
- 超过时必须按职责拆分

## DRY 原则

相同的代码块出现 **2 次以上**，必须提取为公共函数或组件。

## 最小改动

- 只改任务范围内的文件
- 发现无关 bug 先报告，获确认后再修
