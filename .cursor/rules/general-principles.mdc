---
description: 通用开发原则，适用于公司所有 Web 项目，始终生效
alwaysApply: true
---

# 通用开发原则

AI 在编写或修改代码时必须严格遵守。

## 圈复杂度控制

单个函数/方法的圈复杂度不得超过 **10**。超过时必须拆分为多个具名函数。

## Early Return 原则

- 禁止超过 **3 层**的 if 嵌套
- 所有前置条件校验必须使用 Early Return 提前退出
- 复杂条件判断提取为具名布尔变量

## 禁止魔法数字

所有在逻辑中直接出现的数字/字符串字面量，必须定义为具名常量。

## 禁止废弃代码

- 禁止提交注释掉的代码块，直接删除
- 禁止保留未使用的变量、import、函数
- `console.log` 禁止出现在提交的代码中

## 单文件行数限制

- 单个文件不超过 **300 行**（新代码）
- 超过时必须按职责拆分

## DRY 原则

相同的代码块出现 **2 次以上**，必须提取为公共函数或组件。

## 最小改动

- 只改任务范围内的文件
- 发现无关 bug 先报告，获确认后再修
