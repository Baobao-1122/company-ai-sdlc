---
description: 文档与代码同步规范，改行为/接口/部署时必须更新 docs
alwaysApply: true
---

# 文档同步规范

## 必须同步 docs 的情况

| 变更类型 | 应更新的文档 |
|----------|-------------|
| 产品范围/验收标准 | `docs/prd.md` |
| API 路径/参数/响应 | `docs/*-api.md` 或 OpenAPI |
| 目录/代码规范 | `docs/dev-standard.md` |
| 部署/环境变量 | `docs/deploy.md` |
| 测试/Harness 约定 | `docs/testing.md` |
| Agent 执行规程 | `AGENTS.md` |

## 变更记录

文档顶部维护「变更记录」表，每次修改追加一行：

```markdown
| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-07-17 | v1.1 | 新增 xxx 接口说明 |
```

## 禁止

- 只改代码不更新文档
- API 行为变更但 docs 仍写旧口径

## AGENTS.md 维护

以下变更时必须同步 `AGENTS.md`：

- 本地启动命令变化
- 新增合并前必跑命令
- 关键代码路径变化
- 新的 Agent 硬性规程
