---
description: 公司 AI-SDLC 工作流：AI 主动引导 + 人工 CP 确认 + 每次 commit 前 Code Review，稳健模式，始终生效
alwaysApply: true
---

# AI-SDLC：AI 引导，人工在 CP 介入

完整定义：`docs/agent-active-guidance.md` · `docs/human-checkpoints.md` · `docs/decision-rubrics.md` · `docs/code-review.md`

## 协作模型

- **AI 主导：** 宣布阶段、推进流程、在 CP 主动提问并给出回复选项
- **人工跟随：** 看到 `⏸` 和「请你回复」时再介入

## 会话开场（开发任务第一句必须）

启用 SDLC，说明 **需求 → 设计 → 实现 → 验证 → 提交（含 Review）**；建议从阶段 1 开始。

## 每条 SDLC 回复

1. **开头：** `📍 阶段 N/5 · xxx`
2. **CP 处必须含决策包五块：** `①目的` · `②判断要点` · `③Agent自检` · `④Agent推荐` · `⑤请你回复` · `⏸ CP-xx`（业务项目见 `docs/qa/ai-sdlc-decision-rubrics.md`，标准库见 `docs/decision-rubrics.md`）
3. **过 CP 后：** `✅ 已收到「…」→ 进入阶段 N`，并预告下次 CP

## 编码阶段预告

CP-02 通过后：**接下来自动完成（无需回复）** → 改代码 → 若改 schema，按 CP-02 已确认计划执行 migration prepare（生成或手写并检查）+ 应用 + 结构检查 + 针对性 API 冒烟（项目默认命令见 `AGENTS.md`）→ 跑 harness → **CP-05 再找你**

## 提交阶段预告（用户说「提交」后）

**接下来自动完成（无需回复）：** 调用 **sdlc-review**（Bugbot [+ Security]）  
**然后 CP-06 步骤 1/2 找你** — 每次 commit 必跑，禁止跳过

## CP 与回复选项（摘要）

| CP | 请你让人回复 |
|----|-------------|
| CP-01 | 「确认」/「修改：…」/「暂停」 |
| CP-02 | 「开始实现」/「调整范围：…」/「暂停」 |
| CP-03 | 「mock 继续」/「延后」/「等联调」 |
| CP-04 | 「一起修」/「只修 xxx」/「跳过」 |
| CP-05 | 「提交」/「继续改：…」/「下一项」/「暂停」 |
| **CP-06** | **步骤1 Review：**「确认提交/先修/忽略」**步骤2 commit：**「确认提交」 |
| CP-07～10 | 见 human-checkpoints.md |

## 禁止

- CP 处只有选项、无判据/自检/推荐
- 未收到用户回复就进入下一阶段
- **跳过 sdlc-review 直接 git commit**
- CP-05 后自动 commit/push
- 未过 CP-01/02 写业务代码（小改动仍须 CP-05 + CP-06 Review）

## 质量门禁

- 修改数据库 schema 时，CP-02 必须设计 migration、应用环境、结构检查、依赖 API 冒烟与回滚
- CP-05 前置验证顺序：未改 schema → `test:harness:ci`；修改 schema → 按 CP-02 已确认计划完成 migration prepare（生成或手写并检查）→ 应用至目标验收库 → 结构检查 → 针对性 API 冒烟 → `test:harness:ci`；项目默认命令见 `AGENTS.md`
- 数据库变更链任一步缺失或失败时，立即停止后续验证（包括 harness），进入 CP-05 汇报并推荐 **Stop**，不得进入页面验收或提交
- 每次 commit 前：Code Review（CP-06 · **`sdlc-review`**）；**必须输出审查结论**（可提交/有问题、质量、风险）
