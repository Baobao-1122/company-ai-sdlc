---
description: 公司 AI-SDLC 工作流：AI 主动引导 + 人工 CP 确认 + Reflection 互查 + 每次 commit 前 Code Review，稳健模式，始终生效
alwaysApply: true
---

# AI-SDLC：AI 引导，人工在 CP 介入

完整定义：`docs/agent-active-guidance.md` · `docs/human-checkpoints.md` · `docs/decision-rubrics.md` · `docs/code-review.md` · `docs/task-state.md`

## Task State（换 Chat 可续）

- 业务项目任务实例：`.sdlc/task-state.yaml`（规范见 `docs/task-state.md`；读写 Skill **`sdlc-task-state`**）
- **开发任务开场 / 新 Chat / 「续任务」**：Agent **必须先 Read** 已有 state（若存在）
- **过 CP、过 harness/P1/CP-06**：Agent **必须 Update** state；**不新增** mandatory 人工步骤（CP 仍由人确认）

## 协作模型

- **AI 主导：** 宣布阶段、推进流程、在 CP 主动提问并给出回复选项
- **人工跟随：** 看到 `⏸` 和「请你回复」时再介入

## 会话开场（开发任务第一句必须）

启用 SDLC，说明 **需求 → 设计 → 实现 → 验证 → 提交（含 Review）**；建议从阶段 1 开始。

## 每条 SDLC 回复

1. **开头：** `📍 阶段 N/5 · xxx`
2. **CP 处必须含决策包五块：** `①目的` · `②判断要点` · `③Agent自检` · `④Agent推荐` · `⑤请你回复` · `⏸ CP-xx`（业务项目见 `docs/qa/ai-sdlc-decision-rubrics.md`，标准库见 `docs/decision-rubrics.md`）
3. **过 CP 后：** `✅ 已收到「…」→ 进入阶段 N`，并预告下次 CP

## 编码阶段预告

CP-02 通过后：**接下来自动完成（无需回复）** → 改代码 → 若改 schema，按 CP-02 计划执行 migration 链 → 若命中业务数据影响面，按计划做**只读**抽样对账并输出结论与问题（**禁止**改删业务数据）→ 跑 harness → 若命中 P1，须先发 **`🔄 Reflection · P1`** 再跑 Bugbot → **CP-05 再找你**

## Reflection 用户可见（硬性）

进入 **P1** 或 **CP-06** 互查前，Agent **必须**在聊天里标明 `🔄 Reflection`（完整模板见 `docs/code-review.md` §11）。**禁止**静默跑 Bugbot 而不告知「本轮是 Reflection」。

| 标识 | 含义 | 你要做什么 |
|------|------|------------|
| `🔄 Reflection · P1 …` | harness 后早期互查 | **无需回复**，等完成 + CP-05 |
| `ℹ️ Reflection · P1 未触发` | 跳过 P1 | 正常进入 CP-05 |
| `🔄 Reflection · CP-06 …` | commit 前互查 | **无需回复**，等 CP-06 步骤 1 |

## 提交阶段预告（用户说「提交」后）

**接下来自动完成（无需回复）：** 先发 **`🔄 Reflection · CP-06`** → 调用 **sdlc-review**（Bugbot [+ Security]）→ **`🔄 Reflection · CP-06 · 完成`**

**然后 CP-06 步骤 1/2 找你** — 每次 commit 必跑，禁止跳过

## CP 与回复选项（摘要）

| CP | 请你让人回复 |
|----|-------------|
| CP-01 | 「确认」/「修改：…」/「暂停」 |
| CP-02 | 「开始实现」/「调整范围：…」/「暂停」 |
| CP-03 | 「mock 继续」/「延后」/「等联调」 |
| CP-04 | 「一起修」/「只修 xxx」/「跳过」 |
| CP-05 | 「提交」/「继续改：…」/「下一项」/「暂停」 |
| **CP-06** | **步骤1 Review：**「确认提交/先修/忽略」**步骤2 commit：**「确认提交」 |
| CP-07～10 | 见 human-checkpoints.md |

## 禁止

- CP 处只有选项、无判据/自检/推荐
- 未收到用户回复就进入下一阶段
- **跳过 sdlc-review 直接 git commit**
- **Reflection 轮次未用 `🔄 Reflection` 告知用户**（见 `docs/code-review.md` §11）
- CP-05 后自动 commit/push
- 未过 CP-01/02 写业务代码（小改动仍须 CP-05 + CP-06 Review）

## 质量门禁

- 修改数据库 schema 时，CP-02 必须设计 migration、应用环境、结构检查、依赖 API 冒烟与回滚
- 命中业务数据影响面时，CP-02 必须设计**只读**抽样对账计划；CP-05 必须给出结论与问题清单；校验过程禁止 INSERT/UPDATE/DELETE 业务数据；修数另开任务并单独授权
- CP-05 前置验证顺序：未改 schema →（若有数据影响则先只读对账）→ `test:harness:ci` →（若命中 P1）**Reflection · P1**；修改 schema → 按 CP-02 计划完成 migration prepare → 应用 → 结构检查 → API 冒烟 →（若有数据影响则只读对账）→ `test:harness:ci` →（若命中 P1）**Reflection · P1**
- 数据库变更链或只读对账任一步**缺失/无法完成/核对写库**时，立即停止后续验证（含 harness），进入 CP-05 汇报并推荐 **Stop**；对账结论「有差异」但已给出问题清单**不是**步骤失败，仍须跑 harness，是否提交由 CP-05 判定
- 每次 commit 前：Code Review（CP-06 · **`sdlc-review`**）；命中数据影响面时须核验对账证据；**必须输出审查结论**
